<Page x:Class="Activities_Inspector.Pages.TimeIntervals"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:fa="http://schemas.fontawesome.com/icons/"
      xmlns:resx="clr-namespace:Activities_Inspector"
      xmlns:converters="clr-namespace:Activities_Inspector.Converters"
      xmlns:models="clr-namespace:Activities_Inspector.Models"
      xmlns:usercontrols="clr-namespace:Activities_Inspector.UserControls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TimeIntervals"
      DataContext="{Binding TimeIntervalsViewModel, Source={StaticResource Locator}}">

    <Page.Resources>
        <converters:DateTimeFormatConverter x:Key="DateTimeFormatConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" 
                                              Visible="Visible"
                                              NotVisible="Collapsed"/>
        <converters:TimeSpanDurationConverter x:Key="TimeSpanDurationConverter" />
        <converters:CollectionToVisibilityConverter x:Key="CollectionToVisibilityConverter"
                                                    Visible="Visible"
                                                    Collapsed="Collapsed"/>
        <converters:CollectionToVisibilityConverter x:Key="CollectionToVisibilityConverterOpposite"
                                                    Visible="Collapsed"
                                                    Collapsed="Visible"/>
        <converters:BooleanToOpacityConverter x:Key="BooleanToOpacityConverter" />

        <DataTemplate DataType="{x:Type models:UsageInfo}">
            <Grid Width="{Binding ActualWidth, ElementName=MainGrid}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Interval.Start, Converter={StaticResource DateTimeFormatConverter}}"
                           Style="{StaticResource DataTemplateItemStyle}"/>
                <TextBlock Text="{Binding Interval.End, Converter={StaticResource DateTimeFormatConverter}}" 
                           Style="{StaticResource DataTemplateItemStyle}"
                           Grid.Column="1"/>
                <TextBlock Text="{Binding Duration, Converter={StaticResource TimeSpanDurationConverter}}"
                           Style="{StaticResource DataTemplateItemStyle}"
                           Grid.Column="2"/>
                <TextBlock Text="{Binding MachineName}"
                           ToolTip="{Binding MachineName}"
                           Style="{StaticResource DataTemplateItemStyle}"
                           Grid.Column="3"/>
                <fa:ImageAwesome Icon="Solid_ExclamationTriangle"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Center"
                                 Foreground="DarkRed"
                                 Width="16"
                                 ToolTip="Avvio dopo un arresto anomalo"
                                 Visibility="{Binding Interval.StartedAfterCrash, Converter={StaticResource BoolToVisibilityConverter}}"
                                 Grid.Column="4"/>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid Margin="0,5,5,5" x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Command="{Binding ExportCommand}"
                    Content="{x:Static resx:Resources.MainWindows_ExportCommand_Content}"
                    Style="{StaticResource Button_Style}"/>

            <Button Command="{Binding LoadIntervalsCommand}"
                    Content="{x:Static resx:Resources.MainWindows_LoadCommand_Content}"
                    Style="{StaticResource Button_Style}"/>

            <Button Command="{Binding CancelCommand}"
                    Content="Annulla"
                    Style="{StaticResource Button_Style}"
                    Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </StackPanel>

        <Grid Width="{Binding ActualWidth, ElementName=MainGrid}"
              Background="Gray"
              HorizontalAlignment="Left"
              Height="30"
              Margin="0,5,0,0"
              x:Name="Header"
              Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{x:Static resx:Resources.MainWindows_TimeIntervals_Start}" 
                       Style="{StaticResource HeaderFieldStyle}"
                       MouseLeftButtonDown="TextBlock_MouseLeftButtonDown"
                       Grid.Column="0"  />
            <TextBlock Text="{x:Static resx:Resources.MainWindows_TimeIntervals_End}" 
                       Style="{StaticResource HeaderFieldStyle}"
                       MouseLeftButtonDown="TextBlock_MouseLeftButtonDown"
                       Grid.Column="1"  />
            <TextBlock Text="{x:Static resx:Resources.MainWindows_TimeIntervals_Duration}" 
                       Style="{StaticResource HeaderFieldStyle}"
                       MouseLeftButtonDown="TextBlock_MouseLeftButtonDown"
                       Grid.Column="2"  />
            <TextBlock Text="{x:Static resx:Resources.MainWindows_TimeIntervals_MachineName}" 
                       Style="{StaticResource HeaderFieldStyle}"
                       MouseLeftButtonDown="TextBlock_MouseLeftButtonDown"
                       Grid.Column="3"  />
            <TextBlock Text="{x:Static resx:Resources.MainWindows_TimeIntervals_StartedAfterCrash}" 
                       Style="{StaticResource HeaderFieldStyle}"
                       MouseLeftButtonDown="TextBlock_MouseLeftButtonDown"
                       Grid.Column="4"  />
        </Grid>

        <ListBox ItemsSource="{Binding Infos}"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 AlternationCount="2"
                 Width="Auto"         
                 ItemContainerStyle="{StaticResource Lststyle}"
                 BorderThickness="0"
                 Visibility="{Binding Infos, Converter={StaticResource CollectionToVisibilityConverter}}"
                 Grid.Row="2"/>
        <usercontrols:NoContentAvailable Visibility="{Binding Infos, Converter={StaticResource CollectionToVisibilityConverterOpposite}}"
                                         Opacity="{Binding IsBusy, Converter={StaticResource BooleanToOpacityConverter}}"
                                         Grid.Row="2"/>

        <fa:ImageAwesome Icon="Solid_Spinner"
                         Spin="True"
                         SpinDuration="4"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center"
                         Foreground="#FF504f4e"
                         Width="30"
                         Margin="0,0,25,5"
                         Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"
                         Grid.Row="2"/>
    </Grid>
</Page>
